A=1
B=1
arr = list()

if A>0 and B <10:
    while A!=0 and B!=0:
        A,B = map(int,input().split())
        arr.append(A+B)
        
    k=0
    while arr[k] != 0:
        print(arr[k])
        k +=1
